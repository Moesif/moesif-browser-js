
# Building bundles

Install development dependencies: npm install
Build: npm run build

# To bump the version

- edit `config.js`
- rebuild
- deploy to unpkg CDN
- edit the URLs in readme

# local test server

npm run start